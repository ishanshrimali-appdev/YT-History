<%= form_with(model: albumartistmap) do |form| %>
  <% if albumartistmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(albumartistmap.errors.count, "error") %> prohibited this albumartistmap from being saved:</h2>

      <ul>
        <% albumartistmap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :album_id %>
    <%= form.text_field :album_id %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.text_field :artist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
