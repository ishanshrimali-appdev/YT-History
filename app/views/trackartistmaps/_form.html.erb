<%= form_with(model: trackartistmap) do |form| %>
  <% if trackartistmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trackartistmap.errors.count, "error") %> prohibited this trackartistmap from being saved:</h2>

      <ul>
        <% trackartistmap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :track_id %>
    <%= form.text_field :track_id %>
  </div>

  <div class="field">
    <%= form.label :artist_id %>
    <%= form.text_field :artist_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
